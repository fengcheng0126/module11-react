import classes from "./MealItemForm.module.css";

const MealItemForm = (props) => {
  return (
    <form classesName={classes.form}>
      <input />
      <button>+ Add</button>
    </form>
  );
};

export default MealItemForm;
